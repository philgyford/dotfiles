[user]
	name = Phil Gyford
	email = phil@gyford.com
[core]
	#editor = subl -w
	# editor = mvim --nofork
	# editor = atom --wait
	editor = code --wait
	# editor = nova --wait
	excludesfile = /Users/phil/.gitignore_global
[color]
	ui = true
# Added by GitHub for Mac:
[filter "hawser"]
        clean = git hawser clean %f
        smudge = git hawser smudge %f
        required = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
	process = git-lfs filter-process
[init]
	defaultBranch = main
